#include <stdio.h>

int main() {
    for (int i = -200; i <= 100; i++) {
        if (i % 2 != 0) { // Verifica si el número es impar
            printf("%d\n", i);
        }
    }
    return 0;
}




